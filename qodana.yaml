version: "1.0"
linter: jetbrains/qodana-python-community:2024.2
profile:
  name: qodana.recommended

failureConditions:
    severityThresholds:
      any: 50 # Total number of problems in all severities
      critical: 1 # Severities
      high: 2
      moderate: 3
      low: 4
      info: 5

include:
  - name: CheckDependencyLicenses